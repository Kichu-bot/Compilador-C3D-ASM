inicio principal() {
    num limite = 10;
    num iterador = 0;
    num suma = 0;
    num valor = 7;

    mientras (iterador < limite) {
        suma = suma + (iterador * 2);
        iterador++;
    }

    escribir(suma);

}
