inicio principal() {
    num miVariable = 0;

    // 0. CORRECTO
    leer(miVariable);

    // 1. Falta punto y coma
    leer(miVariable)

    // 2. Falta paréntesis final
    leer(miVariable ;

    // 3. Falta paréntesis inicial
    leer miVariable );

    // 4. Faltan ambos
    leer miVariable ;

    // 5. Vacío
    leer();
}